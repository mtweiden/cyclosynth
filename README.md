## TODO
 - Write a function that computes whether $w$ is divisible by $\beta$, where $w, \beta$ are algebraic integers.
 - Write a function that computes the denominator exponent of a number $\frac{w}{\beta^r}$.
 - Write a function that converts an SO3 matrix to a U2 matrix

## Done
 - Write a function that takes a unitary in $U[Z[\zeta_{2n}, \frac{1}{2}]]$ and outputs its associated Bloch sphere rotation matrix in $SO(3)$.
